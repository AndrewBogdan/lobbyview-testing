services:
  db:
    container_name: lobbyview-db
    build:
      dockerfile: Dockerfile
    restart: always
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    environment:
      POSTGRES_USER: postgres
      POSTGRES_HOST_AUTH_METHOD: trust
volumes:
  pgdata:
